# Product Defaults

Use these defaults when the repo is under-specified:
- Next.js App Router + TypeScript
- PostgreSQL + Prisma
- one repo per project
- polling or SSE before websocket complexity
- task-level model selection persisted
- GitHub bootstrap server-side only
- Railway deploy linkage server-side only
