# Anti-Patterns

Do not produce specs that:
- merge CRUD shell, attachments, provider selection, repo bootstrap, and deploy truth in one first slice
- claim preview is complete without backend state
- hide required migrations or contracts
- rely on client-side secrets or direct browser provisioning
